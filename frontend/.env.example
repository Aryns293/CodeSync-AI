# Copy to frontend/.env for local development so the UI talks to your local
# backend instead of the same origin it'll use in production. Omit this file
# (or leave it unset) for production builds - the app defaults to same-origin,
# which is correct since the backend serves this build itself.
VITE_BACKEND_URL=http://localhost:5001
